型 T {
    设 val
    函 造(v) { 此.val = v }
    函 测试(列表参数) {
        设 映射 = {}
        遍历 x 于 列表参数 {
            映射[x] = x
        }
        遍历 k 于 映射 {
            遍历 v 于 列表参数 {
                示("此.val=#{此.val} k=#{k} v=#{v}")
            }
        }
    }
}
设 obj = 造 T(99)
obj.测试([10, 20])
