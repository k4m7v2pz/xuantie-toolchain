// Test 40: type method with explicit param + 此
型 测试类2 {
    设 值
    函 造(v) { 此.值 = v }
    函 深层访问(外部参数) {
        遍历 a 于 [1, 2] {
            遍历 b 于 ["x", "y"] {
                示("此.值=#{此.值} 外部=#{外部参数} a=#{a} b=#{b}")
            }
        }
    }
}

设 obj = 造 测试类2(42)
obj.深层访问("hello")
